// Same plugin chain as the Next.js app's postcss.config.js, expressed as ESM
// because this package is "type": "module".
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
