# Copy to .env before running `npm start`.
# Must match the POSTGRES_URL in the Next.js app's .env.local.
POSTGRES_URL=
API_PORT=8090
# Session cookie signing secret. Same value as the Next.js app's .env.local.
AUTH_SECRET=
