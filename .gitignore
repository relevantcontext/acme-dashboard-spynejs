*.DS_Store
.idea
package-lock.json
node_modules
node_modules/*
admin
repo-scripts
app/*
!app/.gitkeep
dist/*
src/tests/results/*
!dist/.gitkeep
src/scss/scripts/*

# API tier credentials — holds POSTGRES_URL with the database password.
.env
.env.*
!.env.example

# TLS material (webpack dev-server generates its own self-signed cert).
*.pem
*.key
*.crt

# SAM build artifacts
.aws-sam/
